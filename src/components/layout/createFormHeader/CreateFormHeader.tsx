import StyledCreateFormHeader from './CreateFormHeader.style'

const CreateFormHeader = () => {
    return <StyledCreateFormHeader>
        <h2>Create new campaign:</h2>
    </StyledCreateFormHeader>
}

export default CreateFormHeader